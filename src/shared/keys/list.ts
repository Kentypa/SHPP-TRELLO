// import { createQueryKeys } from "@lukemorales/query-key-factory";
// import { listService } from "../services/list-service";

// export const listKeys = createQueryKeys("list", {
//   all: { queryKey: null, queryFn: () => listService.getBoards() },
//   detail: (id: number) => ({
//     queryKey: [id],
//     queryFn: () => listService.getBoardById(id),
//   }),
// });
